.error-page {
  &__container {
    min-height: 100vh;
  }

  &__description {
    margin: 32px 0;
  }
}

.navigation-link {
  width: 140px;
  height: 40px;
  color: var(--white);
  background-color: var(--blue-500);
  border-radius: 16px;
  box-shadow: var(--shadow--md);
}
