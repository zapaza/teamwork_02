.leaderboard {
  &__container {
    background-color: var(--white);
    max-width: 580px;
    min-height: 622px;
    margin: 24px auto;
    box-shadow: var(--shadow--base);
    border-radius: 16px;
  }

  &__header {
    margin: 24px 24px 24px 32px;
  }
}
