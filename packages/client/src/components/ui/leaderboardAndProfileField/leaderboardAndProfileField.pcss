  .leaderboard-input {
    &__container {
      min-width: 532px;
      border-bottom: 1px dashed var(--gray-500);
      padding: 4px 8px;
      margin: 0 24px 16px 24px;
    }
  }
  
  .user-name {
    padding-left: 12px;
    color: var(--gray-500);
  }
  
  .user-score {
    margin-left: auto;
  }
  