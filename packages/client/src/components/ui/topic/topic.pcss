.topic {
  &__container {
    max-width: 598px;
    min-height: 138px;
    padding: 24px;
    border-radius: 16px;
    background-color: var(--white);
    margin-bottom: 12px;
    box-shadow: var(--shadow--base);
  }

  &__header {
    margin-bottom: 12px;
  }
}
