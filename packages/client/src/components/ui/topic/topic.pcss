.topic {
  &__container {
    max-width: 671px;
    min-height: 138px;
    padding: 24px;
    border-radius: 16px;
    background-color: var(--white);
    margin-bottom: 12px;
    box-shadow: var(--shadow--base);
    
    &:hover {
      cursor: pointer;
    }
  }

  &__header {
    margin-bottom: 12px;
  }
}
